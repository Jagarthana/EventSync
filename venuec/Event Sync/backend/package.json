{
  "name": "eventsync-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Event Sync allocation officer API (matches frontend localStore shapes)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "seed": "node scripts/runSeed.js",
    "docker:db": "docker compose up -d",
    "docker:db:down": "docker compose down"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.9.3"
  }
}
